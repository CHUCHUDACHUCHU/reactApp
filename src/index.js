import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  // <React.StrictMode>
  <App />,
  // </React.StrictMode>
);

//뭐야 왜 깃헙 잔디밭..!!!
//회사 유선 네트워크 tls 문제인가,,?
//으흠~ git email이 회사 이메일로 되어있군,,!!!
//해당 폴더만 git email을 내 개인 이메일로 변경하자~
